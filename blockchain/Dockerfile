FROM docker.io/parity/polkadot-omni-node:stable2512-3
# This image packages the checked-in chain spec generated in blockchain/chain_spec.json.
# Rebuild the image after regenerating the chain spec locally.
COPY chain_spec.json /chain/chain_spec.json
