// Deploy scripts update this file after local or testnet deployments.
// Keeping the stub in git makes the frontend self-contained for new clones.
export const deployments: { evm: string | null; pvm: string | null } = {
  evm: null,
  pvm: null,
};
